# 🎯 管理员账号自动初始化说明

## 功能概述

系统现在会在**每次启动时**自动检查并初始化默认管理员账号，你再也不需要手动生成密码哈希了！

## 默认管理员信息

```
用户名: admin
密码:   admin123
邮箱:   admin@bookstore.com
```

## 工作原理

系统启动时会执行以下检查：

### 1. 角色初始化（Order=1）
- 自动创建 `ROLE_USER`
- 自动创建 `ROLE_ADMIN`
- 自动创建 `ROLE_MODERATOR`

### 2. 管理员账号初始化（Order=2）

#### 场景A：管理员账号不存在
✅ **自动创建**默认管理员账号
- 用户名: `admin`
- 密码: `admin123`（自动使用BCrypt加密）
- 分配 `ROLE_ADMIN` 角色
- 启用账号

#### 场景B：管理员账号存在，但密码不匹配
✅ **自动重置密码**为 `admin123`
- 检测到密码不正确
- 自动生成新的BCrypt哈希
- 更新到数据库

#### 场景C：管理员账号存在，且密码正确
✅ **验证通过**，无需任何操作

#### 场景D：管理员账号存在，但缺少ADMIN角色
✅ **自动添加** `ROLE_ADMIN` 角色

## 启动日志示例

### 首次启动（创建管理员）
```
========================================
  检查并初始化角色...
========================================
✓ 已创建 ROLE_USER
✓ 已创建 ROLE_ADMIN
✓ 已创建 ROLE_MODERATOR
✓ 角色初始化完成
========================================
========================================
  检查并初始化默认管理员账号...
========================================
× 管理员账号不存在，正在创建...
✓ 用户实体已创建: id=1
✓ 密码记录已创建
✓ 管理员角色已分配: ROLE_ADMIN
✓ 默认管理员账号创建成功！
========================================
  🎯 管理员登录信息:
  用户名: admin
  密码: admin123
========================================
```

### 正常启动（管理员已存在且密码正确）
```
========================================
  检查并初始化默认管理员账号...
========================================
✓ 管理员账号已存在: admin
✓ 管理员密码验证成功
✓ 管理员角色验证成功
========================================
  🎯 管理员登录信息:
  用户名: admin
  密码: admin123
========================================
```

### 修复启动（密码不匹配）
```
========================================
  检查并初始化默认管理员账号...
========================================
✓ 管理员账号已存在: admin
⚠ 管理员密码不匹配，正在重置为默认密码...
✓ 管理员密码已重置为: admin123
✓ 管理员角色验证成功
========================================
  🎯 管理员登录信息:
  用户名: admin
  密码: admin123
========================================
```

## 如何使用

### 1. 启动后端
```powershell
cd E:\web\online-bookstore-backend
.\mvnw.cmd spring-boot:run
```

### 2. 查看日志
启动日志中会显示管理员登录信息。

### 3. 登录前端
- 打开浏览器访问: `http://localhost:3000`
- 使用默认账号登录:
  - **用户名**: `admin`
  - **密码**: `admin123`

## 自定义配置（可选）

如果你想修改默认管理员信息，可以编辑：
`online-bookstore-backend/src/main/java/com/bookstore/online_bookstore_backend/config/AdminInitializer.java`

修改以下常量：
```java
private static final String DEFAULT_ADMIN_USERNAME = "admin";      // 默认用户名
private static final String DEFAULT_ADMIN_PASSWORD = "admin123";   // 默认密码
private static final String DEFAULT_ADMIN_EMAIL = "admin@bookstore.com"; // 默认邮箱
```

## 安全建议

⚠️ **生产环境部署时，请务必：**
1. 修改默认管理员密码
2. 或者在首次登录后立即通过前端修改密码
3. 考虑禁用此自动初始化功能（注释掉`@Component`注解）

## 相关文件

- **AdminInitializer.java** - 管理员初始化逻辑
- **OnlineBookstoreBackendApplication.java** - 角色初始化逻辑
- **UserAuthRepository.java** - 用户密码数据访问
- **UserRepository.java** - 用户数据访问

## 常见问题

### Q: 如果我忘记了管理员密码怎么办？
**A**: 重启后端应用即可！系统会自动检测密码不匹配并重置为 `admin123`。

### Q: 我可以有多个管理员账号吗？
**A**: 可以！通过注册功能创建新用户，然后在数据库中手动分配 `ROLE_ADMIN` 角色。

### Q: 为什么每次启动都显示管理员信息？
**A**: 这是为了方便开发和调试。如果觉得日志过多，可以修改 `AdminInitializer.java` 减少日志输出。

### Q: 这会影响现有的管理员账号吗？
**A**: 不会！如果admin账号已存在且密码正确，系统只会验证通过，不会做任何修改。

---

✅ **现在你可以放心开发，不用再担心管理员账号问题了！**

