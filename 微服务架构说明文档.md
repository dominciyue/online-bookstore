# 📚 微服务架构说明文档

## 项目名称：E-Book 在线书店 - 作者查询微服务

**作者**：Dominic  
**日期**：2025年11月3日

---

## 📋 目录

1. [项目概述](#项目概述)
2. [Service Registry（服务注册中心）的作用](#service-registry服务注册中心的作用)
3. [Gateway（API网关）的作用](#gatewayapi网关的作用)
4. [项目架构设计](#项目架构设计)
5. [服务部署方式](#服务部署方式)
6. [服务使用方式](#服务使用方式)
7. [总结](#总结)

---

## 1. 项目概述

### 1.1 功能描述

本项目实现了一个**作者查询微服务**，可以根据书名查询作者信息。采用 Spring Cloud 微服务架构，包含以下核心组件：

| 组件 | 端口 | 职责 |
|------|------|------|
| **Eureka Server** | 8761 | 服务注册中心（Service Registry） |
| **Author Service** | 8081 | 作者查询微服务 |
| **Bookstore Backend** | 8080 | 主业务系统 |

### 1.2 技术栈

- **Spring Boot**: 3.2.0
- **Spring Cloud**: 2023.0.0
- **Eureka**: 服务注册与发现
- **OpenFeign**: 声明式服务调用
- **MySQL**: 数据存储

---

## 2. Service Registry（服务注册中心）的作用

### 2.1 什么是 Service Registry？

**Service Registry（服务注册中心）** 是微服务架构中的核心组件，负责**管理所有微服务的注册信息和健康状态**。在本项目中，我们使用 **Netflix Eureka** 作为服务注册中心。

### 2.2 核心功能

#### 2.2.1 服务注册（Service Registration）

**定义**：微服务启动时，自动将自己的信息（IP、端口、服务名）注册到 Eureka Server。

**在本项目中的实现：**

```java
// Author Service 配置
@SpringBootApplication
@EnableDiscoveryClient  // 启用服务注册
public class AuthorServiceApplication {
    public static void main(String[] args) {
        SpringApplication.run(AuthorServiceApplication.class, args);
    }
}
```

```yaml
# application.yml
spring:
  application:
    name: author-service  # 服务名称

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # 注册中心地址
    register-with-eureka: true  # 启用服务注册
```

**流程图：**

```
Author Service 启动
       ↓
读取配置（服务名、端口）
       ↓
连接 Eureka Server (8761)
       ↓
发送注册请求
       ↓
Eureka Server 保存服务信息
       ↓
注册成功，开始发送心跳
```

---

#### 2.2.2 服务发现（Service Discovery）

**定义**：微服务需要调用其他服务时，从 Eureka Server 获取目标服务的地址列表。

**在本项目中的实现：**

```java
// Bookstore Backend 通过 Feign 调用 Author Service
@FeignClient(name = "author-service")  // 使用服务名而非IP地址
public interface AuthorServiceClient {
    @GetMapping("/api/authors/by-book")
    AuthorResponseDTO getAuthorByBookTitle(@RequestParam("title") String bookTitle);
}
```

**调用流程：**

```
Bookstore Backend 需要调用 Author Service
       ↓
向 Eureka Server 查询 "author-service"
       ↓
Eureka 返回所有 author-service 实例列表
       ↓
LoadBalancer 选择一个实例（如 localhost:8081）
       ↓
发起 HTTP 调用
       ↓
返回结果
```

**关键优势**：
- ✅ **解耦**：不需要硬编码 IP 地址和端口
- ✅ **动态**：服务实例可以动态增减
- ✅ **容错**：某个实例挂掉，自动切换到其他实例

---

#### 2.2.3 健康检查（Health Check）

**定义**：Eureka Server 定期检查每个服务的健康状态，自动剔除不健康的实例。

**机制：**

1. **心跳机制**：每个服务每 30 秒向 Eureka 发送心跳
2. **超时剔除**：如果 90 秒内没有收到心跳，Eureka 会将该实例标记为 DOWN
3. **自动恢复**：服务恢复后重新注册，自动加入服务列表

**配置示例：**

```yaml
eureka:
  instance:
    lease-renewal-interval-in-seconds: 5   # 心跳间隔（秒）
    lease-expiration-duration-in-seconds: 10  # 超时时间（秒）
```

**健康检查流程：**

```
Author Service 每 5 秒发送心跳
       ↓
Eureka Server 记录最后心跳时间
       ↓
如果超过 10 秒未收到心跳
       ↓
将实例标记为 DOWN
       ↓
其他服务不再调用该实例
       ↓
服务恢复后重新注册
       ↓
状态变为 UP
```

---

#### 2.2.4 负载均衡（Load Balancing）

**定义**：当同一个服务有多个实例时，Eureka 配合 LoadBalancer 实现自动负载均衡。

**在本项目中的应用场景：**

```
假设我们启动了 3 个 Author Service 实例：
- author-service-1: localhost:8081
- author-service-2: localhost:8082
- author-service-3: localhost:8083

Eureka 会维护这 3 个实例的列表：
┌─────────────────────────┐
│ Service: author-service │
├─────────────────────────┤
│ Instance 1: 8081 (UP)   │
│ Instance 2: 8082 (UP)   │
│ Instance 3: 8083 (UP)   │
└─────────────────────────┘

当 Bookstore Backend 调用时：
请求1 → Instance 1
请求2 → Instance 2
请求3 → Instance 3
请求4 → Instance 1  （轮询策略）
```

**配置：**

```yaml
spring:
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
      # 使用 Spring Cloud LoadBalancer
```

---

### 2.3 Eureka Dashboard（可视化管理）

**访问地址**：http://localhost:8761

**界面展示：**

```
+---------------------------------------+
| Eureka Dashboard                      |
+---------------------------------------+
| System Status                         |
|   - Registered replicas: 1            |
|   - Available replicas: 1             |
+---------------------------------------+
| Instances currently registered        |
+---------------------------------------+
| Application         | Status          |
+---------------------+-----------------+
| AUTHOR-SERVICE      | UP (1)          |
|   - localhost:8081  | Status: UP      |
+---------------------+-----------------+
| BOOKSTORE-BACKEND   | UP (1)          |
|   - localhost:8080  | Status: UP      |
+---------------------------------------+
```

**可视化优势：**
- ✅ 实时查看所有服务状态
- ✅ 监控服务实例数量
- ✅ 查看服务注册时间
- ✅ 快速定位问题服务

---

### 2.4 Service Registry 的重要性总结

| 功能 | 传统方式（无注册中心） | 使用 Eureka（有注册中心） |
|------|---------------------|-------------------------|
| **服务地址** | 硬编码 IP:Port | 使用服务名，动态获取 |
| **新增实例** | 需要修改配置文件 | 自动发现，无需修改 |
| **实例下线** | 需要手动更新配置 | 自动感知，停止调用 |
| **负载均衡** | 需要手动配置 Nginx | 自动客户端负载均衡 |
| **监控** | 需要额外工具 | Dashboard 可视化 |

---

## 3. Gateway（API网关）的作用

### 3.1 什么是 Gateway？

**API Gateway（API网关）** 是微服务架构中的**统一入口**，所有外部请求都先经过网关，再路由到具体的微服务。

⚠️ **注意**：本项目**目前未实现 Gateway**，但我们会说明如果添加 Gateway 会是什么样子。

### 3.2 核心功能

#### 3.2.1 统一入口（Single Entry Point）

**定义**：所有外部请求通过统一的网关地址访问，而不是直接访问各个微服务。

**对比：**

**没有 Gateway（当前架构）：**
```
客户端直接访问各个服务：
- http://localhost:8080/api/books/author  (Bookstore Backend)
- http://localhost:8081/api/authors/health  (Author Service)
```

**有 Gateway（推荐架构）：**
```
客户端只访问网关：
- http://localhost:9000/bookstore/api/books/author  → 路由到 Bookstore Backend
- http://localhost:9000/author/api/authors/health   → 路由到 Author Service

网关统一端口：9000
```

**架构对比图：**

**当前架构（无 Gateway）：**
```
┌─────────┐
│ 客户端   │
└────┬────┘
     │
     ├──────→ http://localhost:8080 → Bookstore Backend
     │
     └──────→ http://localhost:8081 → Author Service
```

**推荐架构（有 Gateway）：**
```
┌─────────┐
│ 客户端   │
└────┬────┘
     │
     ↓ 统一入口
┌─────────────────┐
│  Gateway (9000) │  ← 统一认证、日志、限流
└────┬────────────┘
     │
     ├──────→ Bookstore Backend (8080)
     │
     └──────→ Author Service (8081)
```

---

#### 3.2.2 路由转发（Routing）

**定义**：根据请求的路径，将请求转发到对应的微服务。

**如果实现 Gateway，配置示例：**

```yaml
# Gateway 配置
spring:
  cloud:
    gateway:
      routes:
        # 路由到 Bookstore Backend
        - id: bookstore-route
          uri: lb://bookstore-backend  # lb = load balance
          predicates:
            - Path=/bookstore/**
          filters:
            - StripPrefix=1  # 去掉 /bookstore 前缀

        # 路由到 Author Service
        - id: author-route
          uri: lb://author-service
          predicates:
            - Path=/author/**
          filters:
            - StripPrefix=1
```

**路由流程：**
```
请求: GET http://localhost:9000/bookstore/api/books/author?title=Spring实战
       ↓
Gateway 匹配规则：/bookstore/** 
       ↓
去掉前缀 /bookstore
       ↓
从 Eureka 获取 bookstore-backend 地址
       ↓
转发到: http://localhost:8080/api/books/author?title=Spring实战
       ↓
返回结果给客户端
```

---

#### 3.2.3 统一认证（Authentication）

**定义**：在网关层统一处理用户认证，微服务无需重复验证。

**在本项目中的改进方案：**

**当前方式（无 Gateway）：**
```java
// 每个服务都要处理 JWT 认证
@RestController
public class BookController {
    @GetMapping("/author")
    public ResponseEntity<?> getAuthor(@RequestHeader("Authorization") String token) {
        // 验证 JWT Token
        if (!jwtUtil.validate(token)) {
            return ResponseEntity.status(401).body("未授权");
        }
        // 业务逻辑...
    }
}
```

**使用 Gateway 后：**
```java
// Gateway 统一认证
@Component
public class AuthFilter implements GlobalFilter {
    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        String token = exchange.getRequest().getHeaders().getFirst("Authorization");
        
        // 统一验证 JWT
        if (!jwtUtil.validate(token)) {
            exchange.getResponse().setStatusCode(HttpStatus.UNAUTHORIZED);
            return exchange.getResponse().setComplete();
        }
        
        // 通过认证，继续请求
        return chain.filter(exchange);
    }
}

// 微服务无需再验证，直接处理业务
@RestController
public class BookController {
    @GetMapping("/author")
    public ResponseEntity<?> getAuthor() {
        // 直接处理业务逻辑，无需验证 Token
    }
}
```

**优势：**
- ✅ 认证逻辑集中管理
- ✅ 微服务专注业务逻辑
- ✅ 减少重复代码

---

#### 3.2.4 限流保护（Rate Limiting）

**定义**：限制每个用户的请求频率，防止 API 被滥用。

**配置示例：**
```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: author-route
          uri: lb://author-service
          predicates:
            - Path=/author/**
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10  # 每秒允许 10 个请求
                redis-rate-limiter.burstCapacity: 20   # 突发容量 20
```

**效果：**
```
用户 A 在 1 秒内发送 15 个请求：
- 前 10 个：正常处理 ✅
- 后 5 个：触发限流 ❌ 返回 429 Too Many Requests
```

---

#### 3.2.5 统一日志（Logging）

**定义**：在网关层记录所有请求日志，便于追踪和分析。

**实现示例：**
```java
@Component
public class LoggingFilter implements GlobalFilter {
    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        long startTime = System.currentTimeMillis();
        String path = exchange.getRequest().getURI().getPath();
        
        return chain.filter(exchange).then(Mono.fromRunnable(() -> {
            long duration = System.currentTimeMillis() - startTime;
            int statusCode = exchange.getResponse().getStatusCode().value();
            
            log.info("请求路径: {}, 状态码: {}, 耗时: {}ms", path, statusCode, duration);
        }));
    }
}
```

**日志输出：**
```
2025-11-03 16:30:15 - 请求路径: /bookstore/api/books/author, 状态码: 200, 耗时: 150ms
2025-11-03 16:30:16 - 请求路径: /author/api/authors/health, 状态码: 200, 耗时: 30ms
```

---

#### 3.2.6 跨域处理（CORS）

**定义**：统一配置跨域策略，前端可以访问所有微服务。

**配置示例：**
```yaml
spring:
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowed-origins: "http://localhost:3000"  # 允许的前端地址
            allowed-methods:
              - GET
              - POST
              - PUT
              - DELETE
            allowed-headers: "*"
            allow-credentials: true
```

---

### 3.3 Gateway 的重要性总结

| 功能 | 无 Gateway | 有 Gateway |
|------|-----------|-----------|
| **入口** | 每个服务独立端口 | 统一端口（如 9000） |
| **认证** | 每个服务重复验证 | 网关统一认证 |
| **限流** | 需要在每个服务实现 | 网关统一限流 |
| **日志** | 分散在各个服务 | 网关统一记录 |
| **跨域** | 每个服务单独配置 | 网关统一配置 |
| **监控** | 需要监控多个端口 | 监控一个网关即可 |

---

## 4. 项目架构设计

### 4.1 当前架构（Eureka Only）

```
┌─────────────────────────────────────────────────────┐
│                  Eureka Server                      │
│                  (localhost:8761)                   │
│             服务注册中心 + Dashboard                 │
└───────────────────┬─────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
┌───────▼───────┐       ┌──────▼──────────┐
│ Author Service│       │Bookstore Backend│
│ (port: 8081)  │◄──────│  (port: 8080)   │
│               │ Feign │                 │
└───────┬───────┘       └────────┬────────┘
        │                        │
        └──────────┬─────────────┘
                   │
            ┌──────▼────────┐
            │  MySQL 数据库  │
            └───────────────┘
```

**特点：**
- ✅ 服务自动注册与发现
- ✅ 客户端负载均衡
- ✅ 健康检查
- ❌ 无统一入口
- ❌ 每个服务独立认证

---

### 4.2 推荐架构（Gateway + Eureka）

```
         ┌──────────┐
         │  客户端   │
         └─────┬────┘
               │
               ↓ 统一入口 (port: 9000)
    ┌──────────────────────┐
    │   Spring Cloud       │
    │      Gateway         │
    │  - 路由              │
    │  - 认证              │
    │  - 限流              │
    │  - 日志              │
    └──────────┬───────────┘
               │
               ↓ 查询服务地址
┌──────────────────────────────────┐
│       Eureka Server              │
│       (localhost:8761)           │
└──────────┬───────────────────────┘
           │
    ┌──────┴──────┐
    │             │
┌───▼───────┐ ┌──▼──────────┐
│  Author   │ │  Bookstore  │
│  Service  │ │   Backend   │
└───────────┘ └─────────────┘
```

---

### 4.3 架构对比

| 组件 | 当前架构 | 推荐架构 |
|------|---------|---------|
| **Service Registry** | ✅ Eureka Server | ✅ Eureka Server |
| **API Gateway** | ❌ 无 | ✅ Spring Cloud Gateway |
| **服务发现** | ✅ 通过 Eureka | ✅ 通过 Eureka |
| **负载均衡** | ✅ 客户端 LB | ✅ 网关 LB |
| **统一认证** | ❌ 分散认证 | ✅ 网关认证 |
| **限流保护** | ❌ 无 | ✅ 网关限流 |
| **统一日志** | ❌ 分散日志 | ✅ 网关日志 |

---

## 5. 服务部署方式

### 5.1 部署环境要求

**软件环境：**
- JDK 17
- MySQL 8.0.41
- Maven 3.6+ （可选）
- VSCode + Extension Pack for Java

**硬件要求：**
- 内存：至少 2GB（推荐 4GB）
- 磁盘：500MB 可用空间

---

### 5.2 部署步骤

#### 步骤 1：准备数据库

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS bookstore_db CHARACTER SET utf8mb4;

-- 使用数据库
USE bookstore_db;

-- 创建 books 表（如果不存在）
CREATE TABLE IF NOT EXISTS books (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    author VARCHAR(100),
    isbn VARCHAR(20) UNIQUE,
    publisher VARCHAR(100),
    price DECIMAL(10,2),
    cover VARCHAR(1000),
    description TEXT,
    category VARCHAR(50),
    deleted BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP NULL
);

-- 插入测试数据
INSERT INTO books (title, author, isbn, publisher, price, category) VALUES
('Spring实战', 'Craig Walls', '978-7-115-12345-6', '人民邮电出版社', 89.00, '技术'),
('Java编程思想', 'Bruce Eckel', '978-7-111-12345-6', '机械工业出版社', 108.00, '技术');
```

---

#### 步骤 2：配置项目

**2.1 检查数据库配置**

确保以下文件中的数据库配置正确：

**`author-service/src/main/resources/application.yml`**
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookstore_db
    username: root
    password: your_password  # 修改为您的密码
```

**`online-bookstore-backend/src/main/resources/application.properties`**
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/bookstore_db
spring.datasource.username=root
spring.datasource.password=your_password  # 修改为您的密码
```

---

#### 步骤 3：启动服务

**3.1 使用 VSCode 启动（推荐）**

1. **打开工作区**
   ```
   文件 → 打开工作区 → 选择 bookstore-microservices.code-workspace
   ```

2. **按 F5 启动服务**
   - 选择 `🌐 Eureka Server`，点击启动，等待 30 秒
   - 选择 `📚 Author Service`，点击启动，等待 15 秒
   - 选择 `🏪 Bookstore Backend`，点击启动，等待 20 秒

3. **验证启动**
   - 访问 http://localhost:8761
   - 确认看到 2 个服务已注册

---

**3.2 使用命令行启动**

**终端 1 - Eureka Server:**
```bash
cd eureka-server
mvn spring-boot:run
```

**终端 2 - Author Service:**
```bash
cd author-service
mvn spring-boot:run
```

**终端 3 - Bookstore Backend:**
```bash
cd online-bookstore-backend
mvn spring-boot:run
```

---

#### 步骤 4：验证部署

**4.1 检查 Eureka Dashboard**

访问：http://localhost:8761

应该看到：
```
Application            Status
AUTHOR-SERVICE         UP (1) - localhost:8081
BOOKSTORE-BACKEND      UP (1) - localhost:8080
```

**4.2 测试健康检查**

```bash
curl http://localhost:8081/api/authors/health
```

**预期响应：**
```json
{
  "status": "UP",
  "service": "author-service",
  "timestamp": 1699000000000
}
```

---

### 5.3 部署配置说明

**端口分配：**
| 服务 | 端口 | 用途 |
|------|------|------|
| Eureka Server | 8761 | 服务注册中心 |
| Author Service | 8081 | 作者查询微服务 |
| Bookstore Backend | 8080 | 主业务系统 |
| MySQL | 3306 | 数据库 |

**日志位置：**
- Eureka Server: 控制台输出
- Author Service: 控制台输出
- Bookstore Backend: `logs/business.log`

---

## 6. 服务使用方式

### 6.1 API 接口说明

#### 6.1.1 精确查询作者

**接口地址：**
```
GET http://localhost:8080/api/books/author?title=<书名>
```

**请求示例：**
```bash
curl "http://localhost:8080/api/books/author?title=Spring实战"
```

**响应示例：**
```json
{
  "bookTitle": "Spring实战",
  "author": "Craig Walls",
  "bookId": 1,
  "isbn": "978-7-115-12345-6",
  "publisher": "人民邮电出版社",
  "otherBooks": [
    {
      "id": 2,
      "title": "Spring Boot实战",
      "isbn": "978-7-115-54321-0"
    }
  ]
}
```

---

#### 6.1.2 模糊查询作者

**接口地址：**
```
GET http://localhost:8080/api/books/author/search?keyword=<关键词>
```

**请求示例：**
```bash
curl "http://localhost:8080/api/books/author/search?keyword=Java"
```

**响应示例：**
```json
[
  {
    "bookTitle": "Java编程思想",
    "author": "Bruce Eckel",
    "bookId": 3,
    "isbn": "978-7-111-12345-6",
    "publisher": "机械工业出版社",
    "otherBooks": []
  }
]
```

---

### 6.2 测试脚本使用

**运行 PowerShell 测试脚本：**
```powershell
.\test-author-service.ps1
```

**功能：**
- 自动检查所有服务状态
- 提供交互式测试菜单
- 支持自定义书名查询
- 彩色输出，易于阅读

---

### 6.3 服务调用流程

**完整调用链路：**

```
1. 客户端发起请求
   ↓
   GET http://localhost:8080/api/books/author?title=Spring实战

2. Bookstore Backend 接收请求
   ↓
   BookController.getAuthorByBookTitle()

3. 通过 Feign 调用 Author Service
   ↓
   AuthorServiceClient.getAuthorByBookTitle("Spring实战")

4. Feign 查询 Eureka 获取服务地址
   ↓
   Eureka: author-service → localhost:8081

5. 发起 HTTP 调用
   ↓
   GET http://localhost:8081/api/authors/by-book?title=Spring实战

6. Author Service 处理请求
   ↓
   AuthorController → AuthorService → BookRepository

7. 查询 MySQL 数据库
   ↓
   SELECT * FROM books WHERE title = 'Spring实战'

8. 返回结果
   ↓
   Author Service → Bookstore Backend → 客户端
```

---

### 6.4 错误处理

**404 - 未找到书籍**
```json
{
  "message": "未找到书籍: 不存在的书"
}
```

**503 - 服务不可用**
```json
{
  "message": "作者服务暂时不可用，请稍后再试"
}
```

**解决方法：**
1. 检查 Author Service 是否启动
2. 访问 Eureka Dashboard 确认服务注册
3. 等待 30 秒让服务完全注册

---

## 7. 总结

### 7.1 Service Registry（Eureka）在本项目中的作用

| 功能 | 作用 | 本项目实现 |
|------|------|-----------|
| **服务注册** | 自动注册服务信息 | ✅ Author Service 和 Bookstore Backend 自动注册 |
| **服务发现** | 动态获取服务地址 | ✅ 通过服务名调用，无需硬编码 IP |
| **健康检查** | 监控服务状态 | ✅ 每 5 秒心跳检测 |
| **负载均衡** | 多实例分发请求 | ✅ 支持水平扩展 |
| **可视化** | Dashboard 管理 | ✅ http://localhost:8761 |

**核心价值：**
- 🎯 **解耦**：服务间不依赖具体地址
- 🎯 **动态**：实例可以动态增减
- 🎯 **可靠**：自动故障转移

---

### 7.2 Gateway 的潜在价值（未实现）

如果添加 Gateway，将获得：

| 功能 | 价值 |
|------|------|
| **统一入口** | 简化客户端调用 |
| **统一认证** | 减少重复代码 |
| **限流保护** | 防止 API 滥用 |
| **统一日志** | 便于追踪和分析 |
| **跨域处理** | 简化前端配置 |

**推荐在生产环境中添加 Gateway**

---

### 7.3 架构演进建议

**当前架构适用于：**
- ✅ 学习和理解微服务基础概念
- ✅ 小规模项目（2-5个微服务）
- ✅ 团队规模较小

**建议添加 Gateway 的场景：**
- 📈 微服务数量增加（5个以上）
- 📈 需要统一认证和授权
- 📈 需要限流和监控
- 📈 对外提供 API

---

### 7.4 学习收获

通过本项目，您已经掌握：

1. ✅ **Service Registry 的核心概念**
   - 服务注册与发现
   - 健康检查机制
   - 负载均衡策略

2. ✅ **微服务通信方式**
   - Feign 声明式调用
   - 服务名解析
   - 异常处理

3. ✅ **Gateway 的理论知识**
   - 统一入口的优势
   - 路由转发机制
   - 安全和限流

4. ✅ **实际部署经验**
   - 启动顺序管理
   - 配置管理
   - 问题排查

---

## 附录

### A. 参考资料

- [Spring Cloud 官方文档](https://spring.io/projects/spring-cloud)
- [Eureka Wiki](https://github.com/Netflix/eureka/wiki)
- [Spring Cloud Gateway 文档](https://spring.io/projects/spring-cloud-gateway)

### B. 常见问题

**Q1: 为什么服务无法注册到 Eureka？**
- 检查 Eureka Server 是否启动
- 检查配置文件中的 `eureka.client.service-url.defaultZone`
- 等待 30 秒让服务完全注册

**Q2: 如何扩展服务实例？**
```bash
# 启动第二个 Author Service 实例（使用不同端口）
java -jar author-service.jar --server.port=8082
```

**Q3: 如何添加 Gateway？**
参考 Spring Cloud Gateway 文档，创建新模块并配置路由规则。

---

**文档版本**: 1.0.0  
**最后更新**: 2025-11-03





